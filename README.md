# TRACE crawler

여수·순천과 관련된 설화·민속 텍스트를 공공 문화 데이터 사이트에서 수집해
공통 JSON 스키마로 저장하는 Python 프로젝트입니다.

## 현재 상태

| 수집원 | 방식 | 현재 결과 | 상태 |
| --- | --- | ---: | --- |
| 한국민속대백과사전 | 표제어 검색 API → 상세 API | 9건 | 자동 수집 가능 |
| 지역N문화 | 검색 API → 상세 API | 13건 | 자동 수집 가능 |
| 한국민족문화대백과사전 | 등록한 개별 URL의 설화 섹션 파싱 | 2건 | 여수시·순천시 수집 가능 |
| 한국향토문화전자대전 | 공공데이터포털 API 예정 | 0건 | API 명세·키 확인 필요 |

현재 건수는 2026-08-12 실행 결과입니다. 민속대백과사전은 정확도를 우선해
표제어에 `여수`/`순천`이 들어가는 항목만 기본 수집합니다. 본문 검색 인덱스를
추가하면 228건 규모의 **광의 후보군**을 만들 수 있지만, 다른 지역 사례와 함께
한 번 언급된 전국 공통 항목도 포함되므로 별도 검수가 필요합니다.

## 설치와 실행

Python 3.10 이상이 필요합니다.

```powershell
py -3 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

각 수집기는 독립적으로 실행할 수 있습니다.

```powershell
python crawler_folkency.py
python crawler_nculture.py
python crawler_encykorea.py
python api_grandculture.py
```

운영자 연락처를 User-Agent에 넣으려면 `.env.example`을 `.env`로 복사하고
`CRAWLER_CONTACT_EMAIL`을 실제 연락 가능한 주소로 바꾸세요. `.env`는 Git에
올라가지 않습니다.

결과는 `output/`에 저장되고 HTTP 응답은 `http_cache.sqlite`에 7일간 캐시됩니다.
자동 검색 크롤러는 전체 실행이 성공했을 때 결과를 새 스냅샷으로 교체합니다.
일부 상세 요청이 실패하면 기존 정상 결과를 보존하면서 성공한 항목만 병합합니다.

## 출력 스키마

모든 JSON 레코드는 다음 필드를 사용합니다.

```json
{
  "제목": "항목명",
  "본문": "수집한 텍스트",
  "출처URL": "https://...",
  "출처사이트명": "수집원",
  "수집일": "YYYY-MM-DD",
  "라이선스_출처표기문구": "이용조건 및 출처",
  "데이터유형": "text"
}
```

## 수집원별 주의사항

### 한국민속대백과사전

기본값은 사이트의 표제어 검색 인덱스를 조회한 뒤 상세 API로 전문을 받습니다.
전국 공통 민속에서 여수·순천 사례가 언급된 자료까지 필요하면
`crawler_folkency.py`의 `SEARCH_INDEXES`에 `nfmw_content_2025`를 추가할 수
있습니다. 이 모드는 재현율은 높지만 수동 검수가 필요합니다.
공식 안내에 따르면 텍스트는 공공누리 제2유형(출처표시·상업적 이용금지)입니다.
사진과 동영상은 개별 저작권이 다르므로 이 프로젝트는 텍스트만 저장합니다.

공식 안내: <https://folkency.nfm.go.kr/overview>

### 지역N문화

`여수`와 `순천`으로 검색한 설화 상세 본문을 저장합니다. 콘텐츠별 저작권 주체와
이용조건이 다를 수 있어 결과에 `이용조건 개별 확인 필요`라고 표시합니다. 실제
재사용 전 각 원문 페이지의 저작권 표시를 확인해야 합니다.

### 한국민족문화대백과사전

검색 결과 경로는 robots.txt에서 자동 접근을 금지하므로 검색을 자동화하지 않습니다.
사람이 검색해 확인한 항목 URL을 `crawler_encykorea.py`의 `TARGET_URLS`에 추가합니다.
본문 인용 시 사이트가 안내하는 출처 표기 형식을 사용합니다. 미디어는 개별 표시를
확인해야 합니다.

### 한국향토문화전자대전

공공데이터포털 데이터셋 `15052768`은 항목명·분야·유형·집필자·원문 URL 등의
텍스트 **메타데이터**를 제공합니다. 현재 코드는 본문 필드까지 있다고 가정한
골격이므로 그대로 실행할 수 없게 막아 두었습니다. 서비스키 발급 후 실제 응답을
확인하고, 메타데이터만 수집할지 본문을 합법적으로 확보할 별도 경로가 있는지 먼저
결정해야 합니다.

공식 데이터: <https://www.data.go.kr/data/15052768/fileData.do>

## 테스트

테스트는 네트워크에 접속하지 않고 HTML/JSON fixture와 임시 디렉터리를 사용합니다.

```powershell
python -m unittest discover -s tests -v
```

## 아직 남은 일

- 민속대백과사전 본문 검색 모드가 필요할지 결정하고, 사용할 경우 광의 후보군 검수
- 민족문화대백과사전의 읍·면 등 세부 지역 항목이 더 필요하면 URL 추가
- 지역N문화 각 원문의 재사용 조건 확인
- 한국향토문화전자대전 수집 범위와 API 응답 구조 확정
- 실제 운영 전 연락처와 `educational/non-commercial` User-Agent 문구 재확인
